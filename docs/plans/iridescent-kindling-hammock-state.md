# План воркти `iridescent-kindling-hammock`

> Файл заведён 29.08.2026. Правило: держать `## RIGHT NOW` свежим, тикать `- [ ]` → `- [x]`.
> Имя файла = имя воркти (не ветки) — иначе снапшот перед компактом подставит чужой план.

## RIGHT NOW

- **focus:** беклог пуст — закрыты все заведённые карточки, включая INF-1.
- **last:** PR #11-#18 смержены и задеплоены. Ретенция каталога джоб включена
  (30 дней, ночной cron); первый прогон снесёт 61 каталог из 121 — 412 МБ из 939.
- **next:** ждать j. Единственное, что нельзя закрыть самому, — перевыгрузка кук,
  когда протухнут.

## Задача: ретенция каталога джоб (INF-1)

**Что сделано:** `core/services/retention.py` + ночной cron (`hour=4, minute=41`). Каталог
удаляется целиком при возрасте больше `JOBS_RETENTION_DAYS` (30) и отсутствии ждущих.

**Дефект, который нашёл живой замер, а не 60 тестов и не мутант-килл 5/5.** Через минуту
после деплоя PR #17 посчитал на проде, сколько каталогов попадёт под нож, — и увидел
«держим из-за ждущих: 4» там, где настоящих должков нет вовсе. `_waiting_job_ids` переписал
предикат досыла своими словами и потерял `job.status == failed`; обычная успешная выдача
бота оставляет строку с `delivered_at IS NULL` навсегда, потому что закрывать её на этом
пути некому. То есть каждая успешная загрузка добавляла каталог, который чистка не тронет
никогда, — и со временем она перестала бы чистить вовсе.

**Почему проверки молчали:** отказ тихий и в безопасную сторону (лишнее хранится, ничего не
теряется), а фикстуры сеяли удобную комбинацию (`delivered_at` проставлен) вместо боевой
(`succeeded` + открытая строка). Мутанта «потеря условия `failed`» убивать было нечем —
теста на этот случай не существовало.

**Лечение (PR #18):** не чинить копию, а убрать её — теперь вызывается `pending_job_ids()`.
Проверка, что источник правды действительно общий: мутант в `redelivery.py` роняет тест в
`test_retention.py`.

**Урок шире задачи:** «предикат тот же самый» в комментарии — это намерение, а не факт.
Фактом его делает только вызов чужой функции; переписанное своими словами условие
расходится молча.

## Задача: досыл несостоявшихся доставок

**Что просил j:** «сохранять chat_id + message_id при постановке; при failed помечать должок;
после починки досылать файл реплаем на исходное сообщение». Досылать **автоматически при
восстановлении**, не по команде.

**Почему отдельная таблица, а не колонки в `job`:** дедупликация по фингерпринту делает джобу
общей для всех, кто прислал ту же ссылку. Колонка `chat_id` в `job` хранила бы только
последнего — то есть фича молча теряла бы получателей, ровно тех, ради кого пишется.
Плюс `SQLModel.create_all` создаёт новые ТАБЛИЦЫ, но не добавляет колонки в существующие:
отдельная таблица заводится на проде сама, без миграции.

**Выборка «должок» = `Delivery.delivered_at IS NULL` И `job.status == failed`.** Из этого
следует, что помечать успешную доставку отдельным вызовом не нужно: удачный обычный путь
оставляет джобу `succeeded`, и она в выборку не попадает.

### Шаги

- [x] Разведка: модель `Job`, `process_job`, бот, эндпоинт `/download`, версии aiogram/arq
- [x] Таблица `Delivery` + регистрация из бота
- [x] Сервис досыла: повтор джобы → отправка файла реплаем → отметка
- [x] Триггер: arq cron (раз в час; повтор джобы сам и есть проба «восстановилось ли»)
- [x] Тесты + мутант-килл
- [x] PR, деплой, проверка на живом боте

### Гочи, найденные по дороге

- 🛑 **Регистрацию доставки нельзя выставлять эндпоинтом.** Первая редакция сделала
  `POST /deliveries` с параметром `chat_id` — а `api` слушает `0.0.0.0:8033` без
  аутентификации, то есть кто угодно мог заставить бота слать файлы чужим людям. Поймал
  фоновый security-review после пуша, не автор и не тесты. Лечение — не токен, а удаление
  маршрута: бот живёт в том же процессе и ходил по HTTP сам к себе.
- 🛑 **Мутант-килл поймал два слепых теста** (первый прогон, 2 из 6 мутантов выжили): один
  тест падал из-за отсутствия токена в окружении, а не по проверяемой причине; второй не
  мог увидеть пропавший импорт модели, потому что импортировал её сам в шапке файла.
- `reply_to_message_id` в aiogram 3.31 ещё есть, но правильный путь —
  `reply_parameters=ReplyParameters(..., allow_sending_without_reply=True)`: иначе отправка
  падает целиком, если пользователь удалил своё сообщение.
- Досыл обязан быть ограничен по числу попыток, иначе при долгом простое площадки он
  превращается в вечный долбёж (и в лишний трафик к YouTube с флагованного адреса).

## Открытое по проекту (вне этой задачи)

- [x] **`POST /download` открыт наружу без аутентификации** — закрыто PR #14 биндом на
  `127.0.0.1`, а не токеном: у `/admin` пароля не было вовсе, то есть дыра была шире, чем
  выглядела из карточки. Замер после деплоя: `ss` показывает `127.0.0.1:8033`, запрос с
  внешнего адреса даёт `000`.
- [x] **API-1** — закрыто PR #15. Миграция оказалась мельче названия: переехали имя класса и
  **место** транспортных параметров (`streamable_http_path` теперь у того, кто монтирует).
  Главное в PR — не миграция, а guard: тестов на MCP-слой не было вовсе, поэтому 28.08 отказ
  импорта всплыл только на проде при зелёной сюите.
- [x] **INF-1** — ретенция каталога джоб (PR #17) и фикс общего предиката (PR #18).
- [ ] Куки живут, пока профиль `Profile 2` простаивает; протухнут — перевыгрузить.
      **Это за j:** выгрузить куки за него я не могу.

## Что осталось невозможным (не хвост, а вывод)

Разослать файлы тем **двенадцати**, кто писал боту до починки, нельзя: `chat_id` тогда нигде
не сохранялся, а Telegram не отдаёт боту историю обращений. Список получателей физически не
существует — досыл работает только вперёд, с тех, кто написал после PR #11.
