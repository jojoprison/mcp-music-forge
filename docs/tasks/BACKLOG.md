# BACKLOG — отложенная работа mcp-music-forge (хаб)

> **Что это.** Индекс того, что надо сделать по проекту, но ещё не сделали. Здесь только
> строки-ссылки; сама проблема разобрана в карточке `bugs/<домен>.md`. Закрытая карточка
> уезжает целиком в [`DONE.md`](DONE.md), а строка отсюда снимается — хаб не должен врать.
>
> **Конвенция (агентам):** нашёл отложенную работу → карточка в `bugs/<домен>.md` + строка сюда.
> Формат карточки и правила ссылок — [`bugs/README.md`](bugs/README.md).
>
> 🔢 **Нумерация PROV/WRK/API/BOT/INF — единственный источник это коммиченный `docs/tasks/`.**
> Следующий номер = `max(открытые в bugs/<домен>.md + закрытые в DONE.md) + 1`, присваивается
> **в момент коммита карточки**. Не резервировать номера в чате, памяти агента или плане воркти:
> глобального счётчика там нет, и две параллельные сессии выдадут один номер разным багам.
>
> **Обновлено:** 2026-08-29.

## Домены и префиксы

| Префикс | Файл | Что покрывает |
|---|---|---|
| `PROV` | `bugs/providers.md` | скачивание: `providers/`, yt-dlp, YouTube, SoundCloud |
| `WRK` | `bugs/worker.md` | arq-воркер, очередь, транскод (`workers/`, `transcoder/`) |
| `API` | [`bugs/api.md`](bugs/api.md) | FastAPI и MCP-слой (`api/`, `mcp_music_forge/`) |
| `BOT` | `bugs/bot.md` | телеграм-бот `@yt_sc_mp3_downloader_bot` (`bot/`) |
| `INF` | `bugs/infra.md` | docker, деплой, сервер `coco`, диск, бэкапы |

Файл домена заводится **вместе с первой его карточкой** — пустых заготовок не держим.
Каталог `incidents/` завести при первой прод-аварии (постмортем = что было, карточка = что делать).

---

## 🟠 API и MCP

- **[API-1](bugs/api.md#api-1--mcp-слой-не-работает-на-mcp-2x-держимся-пином-2)** — `mcp_app.py`
  импортирует `FastMCP`, которого нет в mcp 2.x; держимся пином `<2`. Уронило прод при первой
  пересборке образа 28.08 — вместе с api ложится и телеграм-бот, он в том же процессе.
